name: 'ICMS MCP'
type: module
description: 'Exposes ICMS-specific MCP tools (catalog, validation, transactional import, idempotency lookup) consumed by the iqual ai-platform drupal-bridge agent.'
package: 'iqual'
core_version_requirement: ^10 || ^11
dependencies:
  - drupal:system
  - drupal:node
  - drupal:paragraphs
  - drupal:file
  - drupal:media
  - mcp:mcp
